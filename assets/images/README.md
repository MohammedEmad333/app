# Images (optional)

The demo uses emoji as lightweight stand-ins for illustrations (see
`promptEmoji` / `emoji` fields in `assets/data/units.json`). To use real
artwork, add image files here and set `imageAsset` on the relevant
`QuestionOption`s; the question widgets can be extended to render
`Image.asset` when `imageAsset` is present.
